describe('WinnerCheck checkForWinner', () => {
  it('should return undefined if no winner', () => {
    expect('this').toBe('implemented');
  });

  it('should return WINNER_X for winning combinations of VALUE_X for a 3x3 board stored in 1D array packed row first', () => {
    expect('this').toBe('implemented');
  });

  it('should return WINNER_O for winning combinations of VALUE_O for a 3x3 board stored in 1D array packed row first', () => {
    expect('this').toBe('implemented');
  });

  it('should return TIE if no winner and all moves taken', () => {
    expect('this').toBe('implemented');
  });
});
